/** @type {import('next').NextConfig} */
const nextConfig = {
  // 'export' produces a fully static site you can host free on Netlify / GitHub Pages.
  // Remove this line if you deploy on Vercel and want server features later.
  output: 'export',
};
export default nextConfig;
